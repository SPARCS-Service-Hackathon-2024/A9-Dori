spring:
  datasource:
    url: jdbc:mysql://localhost:8080/dori
    username: konu
    password: kurtcobain99
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        default_batch_fetch_size: 100
    open-in-view: true

